import Vue from 'vue'
import Vuex from 'vuex'
import authService from '../services/authService'


Vue.use(Vuex)

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    authService
  }
})
